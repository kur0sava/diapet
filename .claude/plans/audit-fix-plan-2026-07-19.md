# План фиксов по 5-агентному аудиту DiaPet — 2026-07-19

**База:** HEAD `a99a416` (master), tsc 0 / jest 149(1 skip) / eslint 0e·1w / fuzz PASS.
**Источник:** 5-агентный «свежий» аудит (paranoid-codebase / diashizo / logic-reviewer /
diapet-medical-auditor / paranoid-ux). Сводка находок → `memory/project_audit_5agents_2026-07-19.md`.
**Статус:** В РАБОТЕ (ветка `fix/audit-2026-07-19` от `a99a416`). Порядок — сверху вниз, медбезопасность первой.

### Прогресс (2026-07-19 сессия)
- ✅ **БАТЧ 1 ПОЛНОСТЬЮ**: C1 (red-flag ДКА в анкете — `utils/scoring.ts`+тесты), C2 (U-40/U-100
  передозировка ×2.5 в insulin_types.ts+dog-insulin.ts, RU+EN), H1 (рендерер статей: таблицы, h4,
  вложенные списки, склейка подряд-цитат — `utils/articleMarkdown.ts`+тесты). Коммиты `a97aa63`, `1015a69`.
- ✅ **Визуальный пасс (по просьбе пользователя, после глобального редизайна v2.6)**, коммит `e9d2ee2`:
  Icon 'sync-outline'→RefreshCw (был Батч 7); App boot-экран прозрачный→светлый фон (dark-невидимость
  ошибки хранилища); AchievementModal→ScrollView (обрезка при крупном шрифте, был Батч 5.5);
  SuccessToast длинный текст→guard. Системно проверено: все 42 theme.colors токена разрешаются;
  SafeAreaView из 'react-native' не используется; хардкод-hex в экранах — только семантика (ок).
- ✅ **Чистка репо** (`f94ebbd`): удалены AUDIT_FIX_PLAN.md / BUGFIX-PLAN.md / ISSUE_LIST.md (завершённый скрап).
- ✅ **БАТЧ 2 HIGH** (`8a5160a`): H2 (окно кризиса MMKV EMERGENCY_UNTIL — ачивка/хинт не поверх SOS для любого
  триггера), H5 (getFoodVerdict null-жир → не «good», +тест), H3/M8 (бренды РФ синхронизированы с каталогом;
  найдено и исправлено незаявленное противоречие — Farmina «доступен в РФ» ↔ каталог regions EU/UK), H4 (ProZinc 60д).
- ✅ **БАТЧ 3** (`bcf1ca1`): 3.1 (Patterns/Risk-факторы под hasEnoughData), 3.2 (glucose_improving guard по ma3d).
  3.3 (полоса 2.8-3.3) — отложена на медагента (Батч 8).
- ✅ **БАТЧ 4** (`6d4e374`): 4.1 (scheduleGlucoseReminders очередь glucoseQueued), 4.2 (restore троттлинг minIntervalMs 5м).
  weeklySummary «drop» оставлен намеренно. 4.3 LOW (L001-L004) — НЕ сделано.
- ✅ **БАТЧ 5** (`e5069a7`,`e1bc47a`,`d22003a`): 5.4 (слот времени только при подтверждении — Schedule+AddPet),
  5.2 (порог веса ≥7д), 5.6 (тост единицы Dashboard), 5.1 (edit-режим инъекции + injectionRepository.update),
  5.3 (вес в единице пользователя — PetProfile/PetPicker/MoreMenu), 5.5 (сделано в визпассе).
- ✅ **БАТЧ 6** (`2f611f2`): 6.1 («Альтернативы» скрыты для собак), 6.3/M9 (анкета species-aware — hindLimbs не собакам).
- ✅ **БАТЧ 7** (`59c6926`,`1c61229`,`+хвосты`): пустой tel-гвард, React-ключи тегов, M7 (emergencyHigh→vetContactThreshold),
  иконка weight-warning, FoodSelector eslint (0w); **хвосты**: GLOBAL-регион в FeedGuide, orphan-фото AddPet (savedRef+unmount),
  черновик времён Schedule в useEffect, **L001** initStorage singleton-промис. Осознанно НЕ трогал: getGlucoseLevel
  (НЕ мёртвый — живой фолбэк diaryAnalyzer, покрыт тестами); L002/L003 (миграции — риск brick); L5(мед) собачья
  hypoFirstAid (на медагента); QuickAdd FAB (на усмотрение).
- 🔎 **Доп. визуальный пасс (по просьбе пользователя)**: разобран весь ЧАТ (5 экранов) — 1 фикс (FAB insets, `484eb15`).
- ⏭ **ОСТАЛОСЬ**: Батч 8 (медпроверка чисел `diapet-medical-auditor` + девайс-смоук на `111615546K006735`);
  3.3 клинический порог + L5(мед) — на медагента; L002/L003 (миграции) — отдельная осторожная сессия.
  **Статус: tsc 0 / jest 164+1skip / eslint 0e/0w / fuzz(101,777) PASS. 15 коммитов на ветке `fix/audit-2026-07-19`,
  НЕ запушено, НЕ проверено на устройстве.**

## Сквозной принцип (по требованию пользователя)
На КАЖДОЙ правке параллельно проверять **соседние позиции и данные**, а не только точку находки:
- правишь одну статью/запись → пройди весь однотипный набор (все article-файлы, все food-записи, оба вида);
- правишь рендер/гейт/порог → найди все прочие места, где тот же паттерн (grep по вызову);
- трогаешь медтекст/число → отметь на медпроверку (агент `diapet-medical-auditor`), сам числа не выдумывай.
Каждый пункт ниже имеет блок **🔎 Соседи** — что обязательно осмотреть заодно.

## Рабочий регламент
- Ветка `fix/audit-2026-07-19` от `a99a416` (НЕ коммитить в master напрямую).
- После каждого батча: `npx tsc --noEmit` → `npm test` → `npx eslint`. После батчей с логикой репозиториев — fuzz
  `FUZZ_SCENARIOS=400 FUZZ_ACTIONS=80 npx jest userScenarios` (сиды 101 и 777).
- Коммит после каждого батча, сообщение `fix(audit-0719): <батч>`.
- Медтексты/числа НЕ финализировать без прогона `diapet-medical-auditor` (Батч 8).
- Устройство: девайс-смоук после всех правок (телефон `111615546K006735`), чек-лист в конце.

---

# БАТЧ 1 — 🔴 CRITICAL медбезопасность + эмердженси-контент (ПРИОРИТЕТ)

## 1.1 — C1: скоринг анкеты маскирует ДКА
- **Проблема:** `AssessmentScreen.tsx:46-50` `getStage` — линейная сумма (≤5 mild / ≤11 moderate / severe).
  `ketoneSmell`+`vomiting` = 2+2=4, «Иногда» вялость = 5 → **mild** «всё под контролем». Противоречит `dka.ts`/`dog-dka.ts`.
- **Правка:** ввести red-flag поверх суммы. Если `ketoneSmell ≥ 1` И `vomiting ≥ 1` → форс `severe`.
  Рассмотреть вторую комбинацию (отказ от еды >12ч + вялость) как severe. Текст severe-стадии — про немедленный визит.
- **Файлы:** `src/features/assessment/screens/AssessmentScreen.tsx`, тексты `locales/ru.ts:690-694` + `en.ts` (stageDescriptions).
- **🔎 Соседи:**
  - Пройти ВЕСЬ список `QUESTIONS` (`AssessmentScreen.tsx:11-20`): какие ещё пары/одиночки клинически «красные»
    (по `dka.ts`, `hypoglycemia.ts`, `dog-dka.ts`, `dog-hypoglycemia.ts`) и не должны тонуть в сумме.
  - Сверить веса баллов: сейчас все вопросы 0/1/2 равновесно — зафиксировать, что red-flag покрывает перекос.
  - Проверить, что severe-текст (RU+EN) реально направляет в клинику, а не «мониторьте».
  - Связка с M9 (ниже): species-aware вопросы — учесть, чтобы red-flag не требовал нерелевантного вопроса.
- **✔ Проверка:** юнит-тест `getStage`-обёртки на кейсах {ketoneSmell:2,vomiting:2,rest:0}→severe; ручной прогон анкеты.
- **⚕ Медпроверка:** да (пороги/комбинации).

## 1.2 — C2: шприцы U-40/U-100 без опасного направления (передозировка ×2.5)
- **Проблема:** `insulin_types.ts:85-94` (RU/EN) и `dog-insulin.ts:45-50` описывают только недодозу
  (U-40 инсулин в U-100 шприц → меньше). Молчат про U-100 инсулин в U-40 шприц → **передозировка ×2.5** = смертельная гипо.
- **Правка:** дописать оба направления, акцент — переизбыток опаснее; связать с разделом гипогликемии.
  Один и тот же текст-шаблон в двух файлах → править согласованно (кошки+собаки).
- **Файлы:** `src/features/encyclopedia/data/articles/insulin_types.ts`, `dog-insulin.ts` (RU и EN блоки).
- **🔎 Соседи:**
  - Проверить, нет ли похожего «однобокого» объяснения концентраций в других статьях
    (`glucose_monitoring.ts`, `dog-insulin`, hints в `aiSystemPrompt.ts`).
  - Сверить перечни U-40/U-100 инсулинов в `speciesConfig` / insulin-таблицах на согласованность концентраций.
- **✔ Проверка:** открыть обе статьи в приложении, убедиться в читаемости (см. H1 — рендер!).
- **⚕ Медпроверка:** да (формулировка + арифметика ×2.5).

## 1.3 — H1: markdown-рендерер статей ломает таблицы / #### / вложенные списки
- **Проблема:** `ArticleDetailScreen.tsx` `renderContent` (149-247) знает только `#/##/###`, `> `, `---`, `- `,
  нумерацию, `**bold**`. Нет: таблиц `|...|`, `#### `, `trimStart()` для отступных списков. → сырой текст в ~13 статьях,
  включая **аварийные таблицы дозы мёда** (`hypoglycemia.ts:55-59`, `dog-hypoglycemia.ts:49-52,113-116`) и 17 подзаголовков
  `insulin_types.ts`. h4 ещё и не в TOC (`:88-92`).
- **Правка (рендерер, предпочтительно — покрывает всё разом):**
  1. Ветка для строк на `|`: рендер простой таблицы (header/rows), разделитель `|---|` — скрыть.
  2. Обработка `#### ` (и `##### ` при наличии), `trimStart()` перед проверкой заголовков → в TOC добавить h4.
  3. `const trimmed = line.trimStart()`; матчить `- `/нумерацию по `trimmed`, глубина отступа → вложенность (•/◦).
  - Альтернатива (если рендер рискован): привести данные к поддерживаемому подмножеству — но тогда потеряем таблицы, хуже.
- **Файлы:** `src/features/encyclopedia/screens/ArticleDetailScreen.tsx` (рендер + TOC).
- **🔎 Соседи (ОБЯЗАТЕЛЬНО — не только 13 названных):**
  - Прогнать **ВСЕ 41 статью** (`data/articles/*.ts`) grep-скриптом на `|`, `#### `, `^\s+- `, `^\s+\d+\.`
    → полный список пострадавших, а не только замеченные агентом.
  - Проверить рендер `> ` цитат и `---` внутри таблиц/после них (не сломать существующее).
  - Сверить, что TOC-anchor’ы уникальны при добавлении h4 (React-ключи).
  - После фикса — глазами открыть эмердженси-статьи (обе гипогликемии, оба вида) и `insulin_types`.
- **✔ Проверка:** визуально в приложении + снапшот-тест `renderContent` на образцах (таблица/h4/вложенный список).
- **⚕ Медпроверка:** ЧИСЛА в таблицах (дозы мёда по весу, стоимости, сроки годности) — Батч 8, `diapet-medical-auditor`.

---

# БАТЧ 2 — 🟠 HIGH (остальные)

## 2.1 — H2: ачивка-модалка поверх Emergency
- **Проблема:** `useHintTrigger.ts:35-39,45-47` подавляет `checkAchievements` только для `trigger==='glucose'`.
  При feeding/injection во время гипо-кризиса золотая модалка (`AchievementModal`, нативный Modal поверх всего) всплывает
  через 3с поверх первой помощи.
- **Правка:** гейтить `checkAchievements` по «есть активный emergencyAlert / текущий роут = Emergency» для ЛЮБОГО триггера,
  а не по типу триггера. Источник состояния — тот же, что рисует Emergency (RootNavigator / emergency store).
- **Файлы:** `src/features/hints/hooks/useHintTrigger.ts` (+ откуда берётся emergency-состояние).
- **🔎 Соседи:**
  - Проверить ВСЕ точки, где ставятся хинты/ачивки/тосты (`HintProvider`, `SuccessToast`, баннеры) — не всплывают ли они
    тоже поверх Emergency.
  - Убедиться, что подавление — это отложка (queue), а не потеря ачивки: после закрытия Emergency она должна показаться.
- **✔ Проверка:** сценарий гипо → лог кормления → Emergency открыт → ачивка не появляется, после выхода — показывается.

## 2.2 — H3: противоречие по Hill's в РФ
- **Проблема:** `cost-planning.ts:63` (RU) «Hill's m/d нерегулярно, x2-3» ↔ каталог `diabeticFoods.ts` (D8: снят из RU, санкции).
- **Правка:** синхронизировать с каталогом — убрать Hill's из RU-раздела cost-planning ИЛИ пометить «недоступен в РФ (санкции), см. каталог».
- **Файлы:** `src/features/encyclopedia/data/articles/cost-planning.ts` (RU+EN), сверка с `diabeticFoods.ts`.
- **🔎 Соседи:**
  - Пройти ВСЕ упоминания брендов в статьях (`dog-diet.ts` — H4-related M8, `diet.ts`, `dog-cost-planning.ts`,
    `remission.ts`) и сверить регион-доступность с каталогом `diabeticFoods.ts` (единый источник истины).
  - Farmina/Royal Canin/Purina — проверить, что оговорки согласованы (M8 внизу — тот же класс).
- **⚕ Медпроверка:** да (актуальность санкц-статуса брендов).

## 2.3 — H4: неподтверждённый ProZinc 20мл=80 дней
- **Проблема:** `insulin_types.ts:42,132` + `aiSystemPrompt.ts:200` утверждают дифф. срок по объёму флакона. Не подтверждено.
- **Правка:** до подтверждения инструкцией BI — унифицировать до единого консервативного срока (60 дней), как Caninsulin/Lantus/Levemir.
- **Файлы:** `insulin_types.ts` (RU+EN), `src/features/hints/data/aiSystemPrompt.ts:200`.
- **🔎 Соседи:**
  - Проверить сроки годности ВСЕХ инсулинов в `insulin_types.ts`/`dog-insulin.ts` на «объём-зависимые» claim’ы.
  - Сверить дубли инсулин-фактов между статьёй и `aiSystemPrompt.ts` (частый рассинхрон).
- **⚕ Медпроверка:** да (нужна инструкция BI ProZinc SPC/PI).

## 2.4 — H5: getFoodVerdict трактует null-жир как «low» (собаки)
- **Проблема:** `diabeticFoods.ts:1556-1574` — `lowFat = fatDM == null || fatDM <= ...` → при `fatDM==null` + высокая клетчатка = «good».
  Латентно: текущие записи все с `fatDM`; риск — калькулятор DMB и сторонние вызовы без жира.
- **Правка:** при `fatDM == null` не давать лучше `'acceptable'` + UI-пометка «жир не указан — уточните у производителя».
- **Файлы:** `src/features/encyclopedia/data/diabeticFoods.ts` (getFoodVerdict), потребители `calculateDryMatter.ts:71`, UI-вердикт.
- **🔎 Соседи:**
  - Проверить симметрично кошачью ветку `getFoodVerdict` на аналогичные null-допущения (углеводы/клетчатка null).
  - Пройти ВСЕ записи `diabeticFoods.ts` (45 bundled) — реально ли у всех задан `fatDM`/макросы (скрипт-валидатор).
  - Проверить путь калькулятора: может ли UI отдать `fatDM=undefined/0` (пустое поле) → как отражается в вердикте.
- **✔ Проверка:** юнит на getFoodVerdict с `fatDM:null` (dog) → не 'good'.

---

# БАТЧ 3 — 🟡 MEDIUM: логика / анализатор

## 3.1 — Анализатор: «мало данных» одновременно с таблицей факторов + «Сомоджи»
- **Проблема:** `AnalyzerDashboardScreen.tsx:284` (Patterns) и `:347` (Risk Breakdown) пропустили гейт `hasEnoughData &&`.
  При 1-2 замерах видно и «Недостаточно данных», и таблица (glucose_stability 50…), и карточка Сомоджи (срабатывает на 2 замерах).
- **Правка:** обернуть оба блока в `hasEnoughData && (...)` (как уже сделано для insight/RiskWidget/трендов :144-152).
- **Файлы:** `src/features/analyzer/screens/AnalyzerDashboardScreen.tsx`.
- **🔎 Соседи:**
  - Прочесать ВЕСЬ экран: любые другие блоки, считающие по `riskScore`/`patterns`/`readings`, тоже под гейтом?
  - Проверить `useAnalyzer.ts:182` (`hasEnoughData` = readings ≥ 3) — консистентно ли с `detectSomogyi` (2 замера)?
    Возможно поднять минимум для паттернов.
- **✔ Проверка:** экран с 1 и 2 замерами → только «Недостаточно данных», без таблиц.

## 3.2 — «Глюкоза улучшается» во время кулдауна low-streak при сползании в гипо
- **Проблема:** `smartAlerts.ts:218-231` `glucose_improving` может выиграть, когда `glucose_low_streak` (:141-154) в 7-дн кулдауне,
  даже если `movingAverage3d < targetLow` (7-дн среднее лагает). → похвала при гипо-тренде.
- **Правка:** в кандидат `glucose_improving` добавить гвард: не показывать, если `movingAverage3d < targetLow`
  (или если недавно был low_streak/низкий надир). Проверить приоритеты выбора (:234-238).
- **Файлы:** `src/features/analyzer/engine/smartAlerts.ts`, при необходимости `trendEngine.ts:76-96` (calculateDirection не смотрит уровень).
- **🔎 Соседи:**
  - Пройти ВСЕ позитивные/низкоприоритетные алерты в `smartAlerts.ts` — нет ли других, что могут «похвалить» при опасном тренде.
  - Проверить взаимодействие кулдаунов (`ALERT_COOLDOWN_DAYS`) — не глушат ли важные алерты дольше, чем надо.
- **✔ Проверка:** юнит smartAlerts на серии {ma7d чуть падает, ma3d < targetLow} → improving НЕ выбран.

## 3.3 — Полоса 2.8-3.3 «treat immediately» без аварийного алерта
- **Проблема:** `types.ts:98-106` `low`=2.8-3.3 «hypoglycemia, treat immediately» (красный бейдж `#FF3B30`), но
  `safetyGuard.ts:98-144` `checkEmergencyThresholds` поднимает hypo только при `< emergencyLow` (2.8).
  `LogGlucoseScreen.tsx:331,485` — тоже только `< emergencyLow`. Замер 3.0 = красный, но без алерта/баннера.
- **Правка (осторожно, требует медрешения):** согласовать шкалу и порог алертинга. Варианты:
  (а) поднять предупреждающий (не severe) алерт для полосы 2.8-3.3 — мягкий баннер «низкий сахар, действуйте»;
  (б) либо уточнить формулировку шкалы, если 2.8-3.3 не требует немедленного действия. НЕ менять `emergencyLow` без медагента.
- **Файлы:** `src/features/analyzer/engine/safetyGuard.ts`, `LogGlucoseScreen.tsx`, тексты; сверка `speciesConfig.ts:226-227,398-399`.
- **🔎 Соседи:**
  - Свести ВСЕ пороги гипо в одном месте: `emergencyLow`(2.8), `severeLow`(2.2), полосы `low`/`severe_low`, оба вида.
    Проверить, что словари тяжести не расходятся (Low-находка #4: `severe_low`<2.8 vs `severe_hypoglycemia`<2.2).
  - Симметрично проверить верхний край (hyper/very_high) на такой же разрыв «бейдж vs алерт».
- **⚕ Медпроверка:** ДА, обязательно — это клинический порог.

---

# БАТЧ 4 — 🟡 MEDIUM: рантайм / уведомления

## 4.1 — scheduleGlucoseReminders теряет конкурентный вызов
- **Проблема:** `useNotifications.ts:387-402` — только boolean-гвард `schedulingGlucose`, без очереди. Вызов во время выполнения
  молча отбрасывается (в отличие от `restoreScheduleNotifications:246-260` с `restoreQueued`-повтором).
- **Правка:** зеркалить паттерн: `glucoseQueued`-флаг + `do{...}while(glucoseQueued)`.
- **Файлы:** `src/shared/hooks/useNotifications.ts`.
- **🔎 Соседи:**
  - Проверить ВСЕ re-entrancy-гварды в `useNotifications.ts`/`hintScheduler.ts`/`weeklySummaryScheduler.ts` — где ещё
    «drop» вместо «queue» (weekly push, hint schedule).
  - Сверить с уже сделанными в v2.6 гвардами (L4 из community-аудита) — не дублировать/не конфликтовать.
- **✔ Проверка:** сложно юнитом; логический разбор + ручной сценарий toggle во время foreground-restore.

## 4.2 — restoreScheduleNotifications на каждый foreground (батарея)
- **Проблема:** `App.tsx:79-85` + `useNotifications.ts:300-349` — полный cancel→reschedule цикл на каждый AppState→active.
- **Правка:** троттлинг restore (не чаще раз в N минут) ИЛИ dirty-флаг (ставит EditPet/Schedule при изменении расписания).
- **Файлы:** `src/core/App.tsx`, `src/shared/hooks/useNotifications.ts`.
- **🔎 Соседи:**
  - Проверить, что при dirty-подходе ВСЕ мутаторы расписания ставят флаг (AddPet, EditPet, ScheduleScreen, delete-pet, toggles в Settings).
  - Не сломать первый холодный restore (должен пройти хотя бы раз).
- **✔ Проверка:** лог частоты restore при переключении приложений.

## 4.3 (LOW, сюда же) — прочие рантайм-хардения
- L001 `initStorage` → shared-promise singleton (как `dbInitPromise`) — `storage.ts:37-75`.
- L002 отмена уведомлений по `data.type` пропускает untagged → на bump versionCode один раз `cancelAllScheduledNotificationsAsync()`.
- L003 defensive: `user_version==0` + существует sentinel-таблица → трактовать как upgrade, добавить миграц-тест.
- L004 `App.tsx:166-172` — `storageError` только при падении `initStorage()`, прочие шаги в свои try/catch.
- **🔎 Соседи:** прогнать `migrationUpgradePath.test.ts` + fuzz на реальных миграциях после L003.

---

# БАТЧ 5 — 🟡 MEDIUM: UX

## 5.1 — LogInjection без режима редактирования
- **Проблема:** `LogInjectionScreen.tsx` нет `editId`; `InjectionListScreen.tsx:85-123` тап по строке — no-op (только delete).
  Опечатку дозы (4 вместо 0.4) правят только «удалить+завести».
- **Правка:** добавить `editId` в LogInjection (по образцу LogGlucose/AddExpense) + `onPress`→edit из InjectionList.
- **🔎 Соседи:**
  - Сверить edit-паттерн с LogGlucose (`editId`, префилл, guard будущего времени, savingRef) — сделать идентично.
  - Проверить, что edit не ломает слияние инъекций в анализатор (`useAnalyzer.ts:86-98`) и double-dose guard.
- **⚕ Медпроверка:** нет (UI), но проверить, что edit-доза проходит те же warn/danger пороги.

## 5.2 — Weight-loss warning без учёта интервала
- **Проблема:** `WeightHistoryScreen.tsx:282` — `deltaKg/prev < -0.05` без времени → шум измерения = красное «срочно к ветеринару».
- **Правка:** показывать `lossWarning` только если между записями ≥ N дней (напр. 7).
- **🔎 Соседи:** проверить логику трендов веса целиком (график, дельта-бейдж ▼/▲), нет ли других время-слепых порогов.
- **⚕ Медпроверка:** порог % и окно дней.

## 5.3 — Единица веса меняется молча из 4 мест + PetProfile хардкодит кг
- **Проблема:** `WeightHistoryScreen.tsx:160-165` + WeightUnitToggle (PetInfo/AddPet/EditPet) — глобальный `setWeightUnit`;
  `PetProfileScreen.tsx:232-234` всегда `кг`.
- **Правка:** привести к единому источнику единицы и применить в PetProfile; ЛИБО локальный тумблер как для глюкозы в LogGlucose.
- **🔎 Соседи:** найти ВСЕ отображения веса (grep `weightKg`, `кг`, `lb`) и привести к выбранной единице; проверить, что хранение
  всегда в кг (канонизация `inputToKg` — подтверждено logic-агентом, не сломать).
- **✔ Проверка:** переключить lb → вес консистентен во всех экранах.

## 5.4 — «Добавить время» + отмена пикера = мусорный слот
- **Проблема:** `ScheduleScreen.tsx:66-87`, `AddPetScreen.tsx:149-165` — `addTime` добавляет слот ДО пикера; dismiss на Android оставляет слот.
- **Правка:** добавлять слот только в `onChange` при наличии `date` (подтверждённый выбор).
- **🔎 Соседи:** проверить оба экрана (Schedule + AddPet), и что удаление слота/дубли времён обрабатываются; связать с L4 (черновик времён пишется только на «Далее»).

## 5.5 — AchievementModal без ScrollView (обрезка при макс. шрифте)
- **Проблема:** `AchievementModal.tsx:34-68` фикс. карточка. При системном max-шрифте на 360×640 кнопка «Закрыть» за краем.
- **Правка:** обернуть содержимое в `ScrollView` c `maxHeight`.
- **🔎 Соседи:** прочесать прочие модалки/оверлеи (HintCard, SuccessToast, PetPickerSheet, confirm-диалоги) на тот же риск при крупном шрифте/360×640 (продолжение размерного аудита 07-16).

## 5.6 — Тумблер единицы глюкозы на Dashboard без обратной связи
- **Проблема:** `DashboardScreen.tsx:141-146` swap-чип молча флипает mmol/L↔mg/dL глобально.
- **Правка:** добавить тост/haptics (как в Settings ToastAndroid).
- **🔎 Соседи:** сверить со сменой единицы в Settings (`:302`) и инвалидацией кэша (`Dashboard:145`) — единица меняется согласованно.

---

# БАТЧ 6 — 🟡 MEDIUM: данные / species-согласованность

## 6.1 — Собаки видят пустой экран «Альтернативы»
- **Проблема:** `alternativeFoods.ts` — все 8 записей cat (species дефолт). `FeedGuideScreen.tsx:132-153` показывает карточку всем,
  `FeedGuideAlternativesScreen.tsx:36-44` фильтрует → пусто для собак.
- **Правка (выбрать):** (а) скрыть карточку для собак по образцу `showNaturalFood` (`:39`); ЛИБО (б) добавить собачьи альтернативы.
  Рекоменд.: (а) сейчас (быстро, честно), (б) — в контент-бэклог.
- **🔎 Соседи:**
  - Пройти ВСЕ хаб-карточки FeedGuide (`FeedGuideScreen`) — какие ещё показываются виду, для которого пусты (natural уже скрыт — эталон).
  - Проверить species-теги во ВСЕХ данных: `alternativeFoods.ts`, `naturalFoods.ts`, `diabeticFoods.ts`, `regionStores.ts`.

## 6.2 — M8: Hill's в dog-diet.ts без региональной оговорки (см. также H3)
- **Правка:** добавить оговорку по образцу Farmina/Royal Canin. Делать вместе с 2.2 (H3) — один проход по брендам.

## 6.3 — M9: вопрос анкеты про стопохождение не species-aware
- **Проблема:** `AssessmentScreen.tsx:11-20` `QUESTIONS` без species; `hindLimbs`/стопохождение редко у собак
  (расходится с `speciesConfig` symptoms.available: cat есть `hindLimbWeakness`, dog нет).
- **Правка:** сделать `QUESTIONS` species-aware (фильтр как `symptoms.available`) ЛИБО смягчить текст для собак.
- **🔎 Соседи:** согласовать с 1.1 (C1 red-flag) — набор вопросов и скоринг должны учитывать вид консистентно;
  проверить весь список вопросов на видо-релевантность.
- **⚕ Медпроверка:** да (релевантность симптома по видам).

---

# БАТЧ 7 — 🔵 LOW / полировка (пакетом)

- **sync-outline иконка** нет в ICON_MAP → `ArticleListScreen.tsx:247` рисует null. Добавить маппинг на `RefreshCw`.
  🔎 Соседи: прогнать grep всех `name="..."` иконок против ICON_MAP (174 ключа) — есть ли ещё сироты (diashizo уже проверил — перепроверить после правок).
- **Граничная 14.0 mmol** — легаси `getGlucoseLevel` (`types.ts:309`) vs `getGlucoseLevelFromRanges`. Мёртвый путь — удалить легаси или согласовать.
  🔎 Соседи: убедиться, что легаси реально нигде не вызывается (grep), обновить `types.test.ts`.
- **orphan-фото при брошенном AddPet** — `AddPetScreen.tsx:73-77` чистит только при замене. Чистить при подтверждённом выходе.
- **Черновик времён Schedule** пишется только на «Далее» (`ScheduleScreen.tsx:105-106`) — писать в `useEffect` при изменении (как LogGlucose). Связано с 5.4.
- **GLOBAL-регион недостижим в FeedGuide** — `FeedGuideScreen.tsx:16-23,42-46` REGIONS без GLOBAL. Добавить вход в GLOBAL-каталог, если регион юзера GLOBAL.
  🔎 Соседи: проверить `getAppRegion()` fallback (`regionConfig.ts:82`) — сколько локалей падает в GLOBAL.
- **Пустой tel: при мусорном номере ветеринара** — `EmergencyScreen.tsx:67`. Если после санитизации пусто → показать поиск клиники.
- **Ключи тегов React** — `ArticleListScreen.tsx:113-115` `key={lang(tag)}` → строить из индекса.
- **eslint-disable съехал** — `FoodSelector.tsx:160 vs 163`. Поправить позицию/deps.
- **L5 (мед) hypoFirstAid AI для собак без весовой градации** — `aiSystemPrompt.ts:102-104` привести к таблице `dog-hypoglycemia.ts`.
  (AI выключен, но текст зашит — правим до включения фичи.)
- **M7 (мед) дубль `emergencyHigh`** в `aiSystemPrompt.ts:69` (иной смысл, чем config) → переименовать в `vetContactThreshold`.
- **weight-warning только цветом** — `WeightHistoryScreen.tsx:283` добавить иконку-!.
- **QuickAdd FAB телепорт на Home** — `QuickAddButton.tsx:33-55` (принять как есть или сохранять контекст — на усмотрение).

---

# БАТЧ 8 — ФИНАЛ: медпроверка чисел + верификация

## 8.1 — Медпроверка (агент `diapet-medical-auditor`)
Прогнать по конкретным числам/протоколам, затронутым в Батчах 1-3,6:
- Дозы мёда по весу (кошка `hypoglycemia.ts:55-59`, собака `dog-hypoglycemia.ts:49-52`) — после починки отображения (H1).
- Пороги 2.8/2.2/полосы (3.3) — решение по 3.3 (Батч 3.3).
- Red-flag комбинации анкеты (C1) + species-релевантность (M9).
- Срок ProZinc (H4) — сверить инструкцию BI.
- Санкц-статус брендов (H3/M8).
- Стоимости в cost-planning таблицах.

## 8.2 — Технический прогон
`npx tsc --noEmit` (0) → `npm test` (все) → `npx eslint` (0e) → fuzz `FUZZ_SCENARIOS=400 FUZZ_ACTIONS=80` сиды 101/777.
Скрипт-валидаторы данных (как у diashizo): все статьи на битый markdown (после H1), все food-записи на макросы/species, i18n ru↔en паритет.

## 8.3 — Девайс-смоук (телефон `111615546K006735`)
- Анкета: ацетон+рвота → severe (не «под контролем»).
- Эмердженси-статьи (обе гипо, оба вида) + insulin_types — таблицы/подзаголовки/вложенные списки рендерятся.
- Гипо-сценарий: лог кормления во время Emergency → ачивка НЕ поверх.
- Анализатор с 1-2 замерами — только «мало данных».
- LogInjection edit; смена единицы веса консистентна; «добавить время»+отмена; AchievementModal при крупном шрифте.
- Собака → «Альтернативы» (скрыто или наполнено).

---

# Приоритеты и границы
- **Сначала:** Батч 1 (C1, C2, H1) — медбезопасность и эмердженси-контент. Затем Батч 2.
- **Медрешения (не трогать без агента):** пороги гипо (3.3), ProZinc-срок, red-flag комбо, дозы, бренд-доступность.
- **Всё это — для ветки `fix/audit-2026-07-19`, не для тега v2.5.0** (релиз 2.5.0 заморожен на `ce5216a`). Куда мёржить (в v2.6-линию
  из ~33 неотправленных коммитов или отдельно) — решить в начале следующей сессии.

Связано: [[project-audit-5agents-2026-07-19]], [[project-audit-3agents-2026-07-10]], [[project-v26-plan-2026-07-17]].
