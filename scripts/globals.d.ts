// Shim for the standalone web-encyclopedia build (scripts/tsconfig.web.json):
// __DEV__ is provided by Expo's type setup in the app tsconfig, which this
// build intentionally does not extend.
declare const __DEV__: boolean;
